import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#F7CA16",
      light: "#F9DB58",
      dark: "#D1A61C",
    },
    secondary: {
      main: "#4549a2",
    },
  },
});

export default theme;
